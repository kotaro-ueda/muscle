.title
  簡単計算
=form_for @number do |f|
  .weight-form__field
    = f.label :重量（kg）, class: 'weight-form__label'
    = f.text_field :weight, class: "weight-form__input", placeholder: "重量を入力してください"
  .parsent-form__field
    = f.label :パーセント, class: 'parsent-form__label'
    = f.text_field :parsent, class: "parsent-form__input", placeholder: "%を入力して下さい"
  .btn
    .btn-form
      = f.submit "計算", class: "btn-form-action"


.result
  適正重量は#{@numbers}kgです

.default
  = link_to "トップページに戻る", root_path, class: "default-btn"